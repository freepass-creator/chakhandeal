# 착한딜(chakhandeal) 환경변수 — 이 파일을 복사해 .env.local 로 만들고 값을 채우세요 (.env.local 커밋 금지)

# ── 매칭키 (서버 전용) ──
# 운영에서는 충분히 긴 랜덤 문자열로 교체하세요. (기본값·미설정 시 운영 기동 실패)
MATCH_HMAC_SECRET=rentsafe-pro-dev-secret-change-me

# ── 데모/레거시 스위치 (운영 기본 OFF) ──
# ALLOW_PUBLIC_CHECK=1   # 이름·생일 공개 조회(/api/v1/check) — 로컬 데모용
# ALLOW_DEMO_LOGIN=1     # 하드코딩 데모 계정 로그인 — 로컬 데모용

# ── Firebase Admin (서버). 없으면 mock 스토어로 동작 ──
# 서비스계정 JSON 전체를 한 줄로, 또는 파일 경로
# FIREBASE_ADMIN_JSON={"type":"service_account",...}
# FIREBASE_ADMIN_PATH=./service-account.json

# ── Firebase 클라이언트 (웹) ──
NEXT_PUBLIC_FB_API_KEY=
NEXT_PUBLIC_FB_AUTH_DOMAIN=
NEXT_PUBLIC_FB_PROJECT_ID=
NEXT_PUBLIC_FB_STORAGE_BUCKET=
NEXT_PUBLIC_FB_MESSAGING_SENDER_ID=
NEXT_PUBLIC_FB_APP_ID=

# ── Gemini OCR (서버 전용) ──
GEMINI_API_KEY=
